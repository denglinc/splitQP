[project]
name = "splitqp"
version = "0.0.0"
description = "One Cholesky, many QPs."
requires-python = ">=3.12,<3.15"
dependencies = [
    "jax>=0.11.0,<0.12",
]

[project.optional-dependencies]
cuda = [
    "jax[cuda13]>=0.11.0,<0.12; sys_platform == 'linux' and platform_machine == 'x86_64'",
]

[dependency-groups]
notebook = [
    "ipykernel>=6,<7",
    "matplotlib>=3.8,<4",
    "nbclient>=0.9,<1",
    "osqp>=1.0,<2",
]

[tool.uv]
package = false
